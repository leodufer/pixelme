'use strict';

/**
 * @ngdoc function
 * @name fortuneApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the fortuneApp
 */
angular.module('fortuneApp')
  .controller('PixelCtrl', function ($scope) {
    $scope.fotos = [];
  });
